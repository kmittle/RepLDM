# TPAMI 扩展工作 TODO（2026-08-31）

本清单是当前唯一执行顺序。2026-08-31 起，研究主线改为用 OPD、DPO 和 RL
训练小型 latent renderer；静态方向只保留为训练基线和评测基础设施。

## 长期执行门槛

- [ ] 所有新实验持续满足干净、清晰、可扩展的工程结构：数据、模型、训练、评测和
  配置分离，公共逻辑不复制，实验差异只通过版本化配置表达。
- [ ] 每个可独立验证的阶段都执行测试和 `$check 1`，随后 commit、push 并核对远端；
  正式实验只能绑定干净且已 push 的 commit，结果必须保存配置、数据哈希和代码版本。

## 已完成的整理

- [x] 将根目录研究文档按 `doc/research/`、`doc/protocols/`、`doc/literature/` 和
  `doc/audits/` 分类。
- [x] 将旧实验输出移到 `outputs/archived_results/`，保留原文件并写明旧结果只能
  用于回顾和排错，不能进入正式结果表。
- [x] 将 2026-08-30 的报告、脚本和图片整体移到 `results/08-30/`。
- [x] 冻结 HPSv2 官方 3,200 行 prompt；保留 28 个重复行，不自行去重。

## 当前正式实验

- [x] 审查全量生成器、完整性审计、严格评分配置和 GPU 等待脚本。队列在启动时
  固定已 push 的 commit，并在生成、审计、评分和分析前重新检查工作区与 commit。
- [x] 修复上一轮 `$check 1` 的 5 个阻塞项：
  - [x] worker 部分启动失败时，停止并回收已经启动的 worker。
  - [x] 每次生成使用独立尝试目录；只有 GPU 监控和模型审计全部通过后，才接受并
    发布这次生成的图片。失败尝试的图片不得用于断点恢复。
  - [x] 评分先写临时文件；成功后发布 `scores.jsonl` 和绑定完整文件哈希的成功回执。
    GPU 冲突、查询失败和分数篡改都必须使分析失败。
  - [x] 所有运行时 `nvidia-smi` 查询都设置 10 秒超时。
  - [x] 只追踪 `outputs/archived_results/README.md`，继续忽略约 14 GB 的旧产物。
- [x] 固定四个实验设置：`no_ag`、`feature_axis_r1_pos`、
  `uniform_axis_r1_pos`、`random_axis_r1_pos`。每个设置生成 3,200 张，四个设置共
  12,800 张；同一 prompt 的四个设置使用同一 seed 和同一 GPU。
- [x] 运行 prompt 单元测试、`compileall` 和 `--validate-only`。验证任务矩阵必须是
  完整的 `3,200 x 4`，不得用子集代替正式评估。
- [x] 完成全部 CPU 回归、配置校验、模型哈希核验和图片迁移复现。全库
  `794/794` 测试通过；任务矩阵仍为 12,800 项，18 文件模型快照和 8 张迁移图片
  的哈希均保持一致。
- [x] 将生成拆成 16 个可独立接受的批次，每卡每批最多 50 个 prompt；后续批次
  失败时，之前已接受的图片仍可继续使用。评分使用内容寻址的私有进度文件和原子
  回执；写回执时中断也不会破坏上一个有效检查点。
- [x] 完成上一轮独立审查发现的阻塞修复：
  - [x] 生成失败时不再等待可能被已退出 worker 占住的发布锁。
  - [x] 正式评分只从经过复制和复验的私有 scorer 资产目录加载，禁止网络回退；
    provenance 必须绑定私有目录中的实际文件哈希。
  - [x] 评分器初始化、GPU 监控或重跑失败时保留上一份有效的 `scores.jsonl` 和成功
    回执；只有私有评分 checkpoint 已落盘并通过校验后，才允许替换旧结果。
  - [x] 补齐资产替换、网络阻断、临时目录清理和旧评分保护的回归测试；真实
    TOPIQ 新旧加载方式的 524 个 state-dict 条目逐项完全一致。
- [x] 在固定测试环境中完成专项测试、全库测试、`compileall`、YAML/shell 校验、
  `git diff --check` 和 `--validate-only`：聚焦测试 `85/85`、全库测试 `803/803`；
  正式矩阵仍为 12,800 项，哈希仍为 `b45e97dd...2f943`。
- [x] 修复 2026-08-31 独立审查确认的全部阻塞项：
  - [x] 用 fsync 的旧结果备份和事务日志保护最终评分发布；进程被强杀或主机掉电后，
    下次启动必须恢复旧的完整结果，或确认新的完整结果已经提交。
  - [x] 将 HPSv2 私有资产加载放入专用 scorer，恢复共享 `scorers/` 的冻结实现，
    不能使已授权的 structural-control 和 fixed-headroom 评分注册失效。
  - [x] 将 OpenAI CLIP 词表和 HPS `ViT-H-14.json` 纳入私有资产、显式加载和哈希；
    将 `ftfy`、`regex` 版本纳入 tokenizer provenance。
  - [x] 正式 scorer 加载前安装 Linux seccomp 网络过滤器；缓存的 Python 函数、
    原生代码和子进程都不能建立网络连接。
  - [x] 更新协议中的无锁 abort、poisoned attempt、崩溃恢复和资产闭包说明。
- [x] 最终修订后在 `repldm_eval`（Python 3.11.10）完成全库 `835/835` 测试、
  `compileall`、YAML/shell 解析、`git diff --check`，并独立全量复验 5.3 GB 开发
  catalog；未启动生成、评分或训练。
- [x] 修复独立审查发现的历史 release 误判：正式构建仍要求 `HEAD` 等于远端 tip，
  后续复验改为要求记录提交仍是记录远端 ref 的祖先。真实临时 Git 图覆盖 tip 相等、
  正常 fast-forward、分叉/force-push、ref 缺失和构建 exact-tip 门槛。
- [x] 修复后全库 `840/840` 测试、`compileall`、45 份 YAML、shell 语法和
  `git diff --check` 通过；重新构建并全量复验开发 release
  `catalog-1d64a300008a089eae44`。15 个 artifact 哈希与冻结配置一致，且
  `--require-training-ready` 仍正确拒绝训练。
- [x] 重新保留并全量复验开发 release `catalog-1aca65a97f6f1be3e312`；随后用
  `--allow-dirty --require-training-ready` 明确确认 metadata-only catalog 不能授权训练。
- [x] selected-view 独立预审返回 `FAIL`：旧保护索引漏掉 4KLSDB 的 2,000 张
  validation 和 1,984 张 test 图片，且八类分层、语义泄漏和图像近重复合同未冻结。
  因此没有启动训练、生成或评分。
- [x] 补入两份 4KLSDB holdout metadata 及 x4 HR 分片索引；保护集现为 49,393 行、
  46,619 条规范化唯一文本、39,712 个带图记录和 37,160 张唯一图片。新增文本与所有
  prompt-bearing 训练 artifact（包括 PixVerve `short_caption`）零精确重合。
- [x] 为跨分片 ID 索引加入 shard/image 计数、缺失 ID、重复 ID、额外图片和重复
  metadata ID 的 fail-closed 检查；专项测试 `37/37`、全库测试 `849/849` 通过，
  `compileall`、39 份 YAML、全部 shell 语法和 staged/unstaged `git diff --check`
  也已通过。
- [x] 用扩展后的保护集重建并全量复验新的开发 catalog
  `catalog-a568ec0ec78a7033fb99`，确认 15 个冻结 artifact。保护集为 49,393 行、
  37,160 张唯一图片，holdout SHA-256 为 `9703fedd...0262cfd`；
  `--require-training-ready` 按预期拒绝该 metadata-only release。旧的
  43,057-row release 只保留为审计证据。
- [ ] 提交修复后执行全新的独立 `$check 1`，取得明确 `PASS`。任何后续代码、配置
  或协议修订都必须重新检查。
- [ ] 提交并 push 到 `origin/rl-version`，核对远端 commit 与本地 HEAD 完全相同。
- [x] 根据 2026-08-31 的方向调整，不启动四设置静态 HPSv2 队列；保留生成、审计、
  评分和恢复代码，供训练后固定 checkpoint 的完整评测复用。

## OPD / DPO / RL 主线

- [x] 整理常用文生图 benchmark、偏好数据和训练方法，明确数据许可、数据泄漏和
  benchmark prompt 排除规则。
- [ ] 完整审计并整理以下 5 个目录的数据来源，不得把两个 `Pixel-Render` 合并计数：
  `/mnt/miah204/bycao/Pixel-Render`、`/mnt/miah204/bycao/Sana`、
  `/mnt/miah209/bycao/Pixel-Render`、`/mnt/miah209/bycao/sana_data`、
  `/mnt/miah209/bycao/sana_runs`。已有可靠元数据的数据仅在 `DATA/*.jsonl` 中引用
  绝对路径；需要补齐元数据的数据先做可复现转换；缓存、日志和 benchmark 只登记并
  明确排除。全量校验条数、路径、重复 ID、许可和评测泄漏。
  - [x] 固定五个物理目录、11 个逻辑数据集和每个来源的条数；确认大 manifest
    5,058,099 行中 3,287,110 个文件存在、1,770,989 个缺失。
  - [x] 许可按 fail-closed 处理：PickScore、OCR、Unsplash 和大 manifest 均不进入
    训练候选；候选总数从 358,173 修正为 313,094（PixVerve 源行 95,735 中有
    95,733 行通过训练资格检查）。
  - [x] 将 Aesthetic-4K 两个 eval split 加入全局 prompt 防火墙；第一版 holdout 为
    43,057 行，并固定各来源文件的字节数与 SHA-256。
  - [x] 发现并补齐本地 4KLSDB validation/test：`caption` 和非空
    `cogvlm_caption` 都受保护，分片 ID 必须与 3,984 张 HR 图片一一对应；holdout
    更新为 49,393 行。
  - [x] 完成 5,058,099 行开发构建，固定并独立复验 15 个 artifact 的字节数与
    SHA-256；扩展保护集后的开发 release 为 `catalog-a568ec0ec78a7033fb99`，且仍
    正确拒绝训练。旧 `catalog-49e0bb3876205f4c5f4e` 只保留为修复前审计证据。
  - [ ] 通过新的独立 `$check 1` 后提交、push，并从远端 commit 重建正式候选目录。
  - [ ] 对首轮实际选中的图片生成完整 selected-payload SHA-256 manifest；在此之前
    `training_ready` 必须保持 false，训练入口必须拒绝运行。
- [x] 审计 `research-opd-renderer-20260828` 的 RPFR/OPD 设计，删除 96 维
  dense-Jacobian、首轮 spatial head、Stage 2、多 scheduler 和无法在四张 3090 上
  验证的部分；在 `OPD_DPO_RL_LATENT_RENDERER_PROTOCOL.md` 冻结 coefficient-only
  renderer、384-state feasibility、QueryLedger 和 OPD/DPO/RL 公平比较合同。
- [x] 冻结首个结构假设 `EulerNativeFrameV1`：六个 canonical raw basis、固定矩
  切空间、Euler scheduler-update 度量白化、可证明的 5% cap、91,654 参数
  coefficient-only renderer、严格零动作和等秩 Haar 随机切空间对照。
- [x] 冻结首个数据视图的规模和配额：nature、urban、people、food、artwork、cgi、
  animals、architecture 八类；每个 `source x stratum` 训练 4 条、验证 2 条，总计 64+32，
  不允许改写文本、重抽 seed 或人工替换。
- [ ] 建立训练数据防火墙：公开偏好数据只用于 reward 或 prompt；排除 HPSv2、
  GenEval 等评测 prompt 和近重复图像，保存排除清单与哈希。
- [ ] 实现共享的 sub-1M scheduler-native basis renderer、同 prompt/同初始噪声
  trajectory 采集，以及严格 no-op / 单次 U-Net / scheduler 坐标测试。
- [ ] 优先实现 OPD：student 先 rollout，teacher 只标注 student 实际访问的 latent；
  分开报告 teacher target gain 和 student realization gain。
- [ ] 在相同 renderer、数据、NFE 和 reward 查询预算下实现 DPO/LPO 偏好训练、
  DRaFT-K/ReFL 可微 reward 训练，以及带 reference/KL 的直接 RL 对照。
- [ ] 每个固定 checkpoint 都跑完整 HPSv2（3,200 图/设置）和 GenEval（2,212
  图/设置）；训练 reward 不能同时充当唯一主评测指标。
- [ ] 每个实验单独报告动机、观察、结果和结论，并报告参数量、GPU 时、reward
  查询数、峰值显存、失败样本和置信区间。

## 论文级验收

- [ ] 补充完整组合能力 benchmark、prompt-disjoint 盲评、跨分辨率和跨模型验证。
- [ ] 用会议版的“latent 结构引导”作为思想联系，清楚区分期刊版的新训练方法，
  同时正面对比 frozen-backbone latent controller 的近期工作。
- [ ] 只有效果、机制证据、消融和统计结论同时成立，才形成 TPAMI 主张。

# Frozen after the registered S3 two-prompt range smoke. This is development
# evidence and retains the full contiguous non-catastrophic intervals.
actions:
  - id: no_ag
    type: none

  - id: conference_expert
    type: legacy
    scale: 0.004
    delay_steps: 3
    decay: [cosine, 0.0, 3]

  - id: raw_0.001
    type: scalar
    scale: 0.001

  - id: moment_tangent_0.002
    type: scalar
    scale: 0.002
    residual_mode: moment_tangent

  - id: trajectory_cone_0.001
    type: scalar
    scale: 0.001
    residual_mode: trajectory_cone_tangent
  - id: trajectory_cone_0.002
    type: scalar
    scale: 0.002
    residual_mode: trajectory_cone_tangent
  - id: trajectory_cone_0.004
    type: scalar
    scale: 0.004
    residual_mode: trajectory_cone_tangent

  - id: trajectory_cone_rescaled_0.001
    type: scalar
    scale: 0.001
    residual_mode: trajectory_cone_tangent_rescaled
  - id: trajectory_cone_rescaled_0.002
    type: scalar
    scale: 0.002
    residual_mode: trajectory_cone_tangent_rescaled

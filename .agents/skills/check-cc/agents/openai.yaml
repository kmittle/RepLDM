interface:
  display_name: "Check RepLDM Dual Review"
  short_description: "Verify changes with two engines and a target"
  default_prompt: "Use $check-cc [n] to verify pending RepLDM changes with Codex and Claude; n is the consecutive clean-pass count (default 2)."
policy:
  allow_implicit_invocation: false
